time frame obfuscation

Certificate pin (chain), not bare --insecure

SNI/the front is "beautiful", but trust only in your partner

uTLS / non-rustls ClientHello (implement separately in Rust)

Exponential backoff when reconnecting to the server

One persistent WSS + internal protocol for TCP (STREAM_OPEN/DATA/WINDOW/CLOSE from PRD or a simplified subset) — replacing N×TLS with N logical streams; common PING/PONG and limits.

A subset of deceptions like creating a new connection, not for every request, but through a random number. By reducing detection by heuristics

Enhance mimicry for the browser

++[https://blog.cloudflare.com/ja4-signals/](https://blog.cloudflare.com/ja4-signals/)++

Параллельные GET малого размера, редко

Умеренный pad/decoy/junk на WSS

< 5-7 процентов трафика 