VERDICT: FAIL

- Rewrite `client_session_reclaims_on_idle_tick` so the map is actually full on a live silent peer before reclaim: keep all 2048 receivers, `await` until a 2049th `Forward` returns the existing cap error, then drop the receivers, wait one idle tick with no further `Forward`, then one new admit must succeed. Today `#[tokio::test]` is current-thread, so `receivers.clear()` runs before the session is polled (map never holds 2048 live slots) and `try_recv().is_err()` after 30ms also passes if the extra `Forward` has not been processed yet.
- In `client_session_ping_aborted_while_send_blocked`, wait until `WS_SEND_BLOCKED_WAITERS > 0` (and assert that) before `session.abort()`. The `|| PING_TASKS_ALIVE > 0` break fires as soon as Ping is spawned (before the 1s period and `maybe_block_ws_send`), so this never covers parent-abort while Ping/send is blocked.
- In `server_ping_aborted_while_send_blocked`, assert `WS_SEND_BLOCKED_WAITERS > 0` before abort; do not abort after a timed wait that can still be zero. Add a pool-enabled repeat of `server_session_abort_drains_inflight_during_blocked_ws_send` (fill `UDP_MUX_SERVER_MAX_INFLIGHT`, abort during blocked WS send, permits return, `JoinSet` empty). Read `server_session_tasks()` before the exit hook that force-stores `0`.
