# JNI libraries

Place `libbibavpn_jni.so` per ABI after building the Rust JNI crate (see `apps/scripts/wsl-build-tauri-android-jni.sh` or `apps/scripts/build-android-jni.ps1`). These binaries are gitignored.
