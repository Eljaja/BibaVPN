# JNI libraries

Place `libbibavpn_jni.so` per ABI after building the Rust JNI crate (see `scripts/wsl-build-all.sh`, `apps/scripts/wsl-build-rust-apk.sh`, or `apps/scripts/build-android-jni.ps1`). These binaries are gitignored.
