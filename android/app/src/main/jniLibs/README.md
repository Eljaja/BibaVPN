# JNI libraries

Place `libbibavpn_jni.so` per ABI after building the Rust JNI crate (see repo root `scripts/wsl-build-all.sh`, `wsl-build-rust-apk.sh`, or `build-android-jni.ps1`). These binaries are gitignored.
